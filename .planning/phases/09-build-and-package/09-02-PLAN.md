---
phase: 09-build-and-package
plan: 02
type: execute
wave: 2
depends_on: [09-01]
files_modified: []
autonomous: false
requirements: [BULD-04, BULD-06]

must_haves:
  truths:
    - "dist/NomisLens.exe launches on Windows 11 without Python installed and exits cleanly with ULTIMATE_ZOOM_SMOKE=1"
    - "All 294+ pytest tests pass after the EXE rebuild"
    - "Source tree (including spec, build.bat, README.md) is visible on https://github.com/fresh-start-git/NomisLens.git"
  artifacts:
    - path: "dist/NomisLens.exe"
      provides: "Single-file portable EXE with corrected hiddenimports"
    - path: "https://github.com/fresh-start-git/NomisLens.git"
      provides: "Public GitHub repo with all 30+ commits pushed"
  key_links:
    - from: "build.bat"
      to: "dist/NomisLens.exe"
      via: "python -m PyInstaller naomi_zoom.spec --noconfirm"
      pattern: "Build complete"
    - from: "dist/NomisLens.exe"
      to: "Windows 11 without Python"
      via: "ULTIMATE_ZOOM_SMOKE=1 smoke test"
      pattern: "exit code 0"
---

<objective>
Human-verify the rebuilt EXE and push the source to GitHub.

Purpose: BULD-04 (EXE runs without Python) and BULD-06 (GitHub push) cannot be automated — they require OS-level isolation and git credentials respectively. This plan surfaces the two manual steps in structured checkpoint form with exact instructions.

Output: Verified dist/NomisLens.exe + NomisLens.git updated to HEAD.
</objective>

<execution_context>
@C:/Users/Jsupport/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Jsupport/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/09-build-and-package/09-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Rebuild EXE and verify it launches without Python</name>
  <what-built>
    Plan 09-01 completed:
    - naomi_zoom.spec now includes PIL._tkinter_finder and win32timezone in hiddenimports
    - build.bat exists at repo root with venv activation and --noconfirm
    - All 8 tests in tests/test_build.py are GREEN
    - Full pytest suite (294+ tests) is GREEN

    This task rebuilds the EXE from the fixed spec and verifies BULD-04.
  </what-built>
  <how-to-verify>
    **Step 1 — Rebuild the EXE:**
    Double-click `build.bat` in the repo root (or run it from a command prompt). Wait for "Build complete. Output: dist\NomisLens.exe" to appear. The build takes ~60-90 seconds.

    **Step 2 — Smoke test the EXE (automatic startup/teardown):**
    Open a command prompt and run:
    ```
    set ULTIMATE_ZOOM_SMOKE=1 && "C:\Users\Jsupport\OneDrive - Ackley Athletics LLC\JB\Naomi Zoom\dist\NomisLens.exe"
    ```
    Expected: The EXE launches, the bubble appears briefly, then closes automatically after ~50 ms. Process exits with code 0. No error dialog. No "Failed to execute script" message.

    **Step 3 — Full launch smoke (optional but recommended):**
    Run the EXE without the smoke flag:
    ```
    "C:\Users\Jsupport\OneDrive - Ackley Athletics LLC\JB\Naomi Zoom\dist\NomisLens.exe"
    ```
    Expected: Tray icon appears, zoom bubble appears on screen, Ctrl+Alt+Z toggles it, right-click tray → Exit closes cleanly.

    **Step 4 — Confirm EXE file size:**
    Check dist\NomisLens.exe file size. Expected: ~28-31 MB (single-file EXE including all deps).

    **What to report:**
    - "approved" if Steps 1-3 pass and no error dialogs appear
    - Describe any error dialogs or unexpected behavior (e.g., "Failed to execute script main", "No module named X", SmartScreen dialog)
  </how-to-verify>
  <resume-signal>Type "approved" or describe the error seen</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Confirm GitHub remote and push source to NomisLens.git</name>
  <what-built>
    All source changes from Phases 7, 8, and 9 (31+ commits) are staged locally in git but have not yet been pushed to the remote.

    IMPORTANT: There is a discrepancy between the REQUIREMENTS.md specification and the live git remote:
    - REQUIREMENTS.md BULD-06 says: https://github.com/fresh-start-git/Ultimate-Zoom.git
    - Live configured remote (git remote -v): https://github.com/fresh-start-git/NomisLens.git

    This checkpoint gives you the chance to confirm which URL is correct before the push.
  </what-built>
  <how-to-verify>
    **Step 1 — Confirm the remote URL:**
    Run in a command prompt from the repo root:
    ```
    git remote -v
    ```
    Expected output contains: `https://github.com/fresh-start-git/NomisLens.git`

    If you intended a different URL (e.g., Ultimate-Zoom.git), update the remote now:
    ```
    git remote set-url origin https://github.com/fresh-start-git/Ultimate-Zoom.git
    ```
    Otherwise, leave the URL as-is (NomisLens.git is the live repo that already has 31 commits synced).

    **Step 2 — Confirm .gitignore is protecting private files:**
    Run:
    ```
    git status
    ```
    Verify that the following do NOT appear in "Changes not staged" or "Untracked files":
    - zoom_log.txt
    - .claude/ (entire directory)
    - .remember/ (entire directory)
    - theme.json
    - config.json
    - dist/ (entire directory)

    **Step 3 — Confirm what will be pushed (optional review):**
    ```
    git log origin/master..HEAD --oneline
    ```
    Expected: 31+ commits listed. Last commit should be the Phase 9 plan commit.

    **Step 4 — Also confirm .planning/ push preference:**
    The .planning/ directory (all design docs, plans, summaries) IS tracked in git and will be pushed. It contains the full project design history. If you prefer to exclude it from the public repo, run:
    ```
    echo .planning/ >> .gitignore
    git rm -r --cached .planning/
    ```
    before pushing. This is optional — the planning docs are useful for future contributors.

    **Step 5 — Push to GitHub:**
    ```
    git push origin master
    ```
    This pushes all 31+ commits (Phases 7, 8, 9 source + all planning docs).

    **Step 6 — Verify on GitHub:**
    Visit https://github.com/fresh-start-git/NomisLens.git (or your confirmed URL) and confirm:
    - naomi_zoom.spec is present
    - build.bat is present
    - README.md is present
    - src/magnifier_bubble/ structure is visible
    - .planning/ directory is present (or absent if you chose to exclude it)

    **What to report:**
    - "approved" if the push succeeded and GitHub shows the expected files
    - Report any push errors (authentication, non-fast-forward, etc.)
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues (remote URL, push error, files that should not have been pushed)</resume-signal>
</task>

</tasks>

<verification>
Phase 9 is complete when both checkpoints are approved:
1. dist/NomisLens.exe smoke test exits 0 with ULTIMATE_ZOOM_SMOKE=1 (BULD-04)
2. git push origin master succeeded; naomi_zoom.spec, build.bat, README.md visible on GitHub (BULD-06)

Combined with Plan 09-01 automated tests (BULD-01, BULD-02, BULD-03, BULD-05), all 6 BULD requirements are satisfied.
</verification>

<success_criteria>
BULD-04: dist/NomisLens.exe launches on Windows 11 without Python installed. ULTIMATE_ZOOM_SMOKE=1 exits cleanly (code 0). No error dialogs on full launch.

BULD-06: Source pushed to https://github.com/fresh-start-git/NomisLens.git (or confirmed alternate URL). naomi_zoom.spec, build.bat, README.md, and src/ visible in the remote repo.
</success_criteria>

<output>
After completion, create `.planning/phases/09-build-and-package/09-02-SUMMARY.md`
</output>
