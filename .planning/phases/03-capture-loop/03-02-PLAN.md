---
phase: 03-capture-loop
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/magnifier_bubble/winconst.py
  - src/magnifier_bubble/window.py
  - src/magnifier_bubble/app.py
  - tests/test_window_integration.py
  - tests/test_capture_smoke.py
autonomous: false
requirements: [CAPT-01, CAPT-02, CAPT-05, CAPT-06]
must_haves:
  truths:
    - "Running the app shows live magnified pixels of whatever is under the bubble inside its content zone"
    - "Moving a window under the bubble updates the magnified view in real time"
    - "The bubble's own teal border does NOT appear in the captured frame (no hall-of-mirrors)"
    - "Memory drift is < 5 MB after 60 seconds of continuous capture (automated) and < 5 MB after 10 minutes (manual)"
    - "Dragging the bubble still works (Phase 2 Pattern 2b drag is not broken by Phase 3 additions)"
    - "Clicking inside the content zone still passes through to the underlying window (Phase 2 HTTRANSPARENT is not broken)"
    - "Closing the bubble cleanly stops the capture worker thread before root.destroy() runs"
  artifacts:
    - path: "src/magnifier_bubble/winconst.py"
      provides: "WDA_EXCLUDEFROMCAPTURE = 0x00000011 constant"
      contains: "WDA_EXCLUDEFROMCAPTURE"
    - path: "src/magnifier_bubble/window.py"
      provides: "BubbleWindow._photo, _image_id, _photo_size, _capture_worker, start_capture(), _on_frame(), SetWindowDisplayAffinity Path A defense, destroy() stops worker"
      contains: "_photo"
    - path: "src/magnifier_bubble/app.py"
      provides: "app.main() calls bubble.start_capture() after BubbleWindow construction"
      contains: "start_capture"
    - path: "tests/test_capture_smoke.py"
      provides: "Windows-only smoke tests: fps >= 30 over 2s, memory drift < 5 MB over 60s, no hall-of-mirrors frame dump"
      contains: "test_capture_worker_achieves_30fps"
    - path: "tests/test_window_integration.py"
      provides: "Additional tests for SetWindowDisplayAffinity, _photo attribute, _image_id canvas item, capture worker lifecycle"
      contains: "test_set_window_display_affinity"
  key_links:
    - from: "src/magnifier_bubble/window.py"
      to: "src/magnifier_bubble/capture.py"
      via: "CaptureWorker construction in start_capture()"
      pattern: "CaptureWorker\\(\\s*state=self\\.state"
    - from: "src/magnifier_bubble/window.py"
      to: "user32.SetWindowDisplayAffinity"
      via: "ctypes call in BubbleWindow.__init__ after ext-style block"
      pattern: "SetWindowDisplayAffinity"
    - from: "BubbleWindow._photo (single ImageTk.PhotoImage)"
      to: "BubbleWindow._on_frame via paste()"
      via: "self._photo.paste(img) on Tk main thread"
      pattern: "self\\._photo\\.paste\\("
    - from: "src/magnifier_bubble/app.py"
      to: "BubbleWindow.start_capture"
      via: "bubble.start_capture() call after construction, before mainloop"
      pattern: "bubble\\.start_capture\\("
---

<objective>
Wire the Plan 03-01 CaptureWorker into BubbleWindow so the content zone shows
live, magnified pixels at >= 30 fps with no hall-of-mirrors and no memory leak,
and run a 7-point manual verification on the Windows dev box to confirm the
Phase 3 user-observable goal (the roadmap's first visible magnified view).

Purpose: Plan 03-01 built the capture producer as an isolated, platform-
independent module. Plan 03-02 builds the CONSUMER side on the Tk main thread
— the single ImageTk.PhotoImage + canvas.create_image item, the _on_frame
callback, the SetWindowDisplayAffinity Path A defense, and the app.py start
call. It also ships the Windows-only smoke tests (fps, memory, hall-of-mirrors)
that the research specifies as the validation gate for CAPT-02 / CAPT-05 /
CAPT-06.

Output:
- winconst.py: WDA_EXCLUDEFROMCAPTURE constant added
- window.py: _photo / _image_id / _on_frame / start_capture / destroy() worker
  teardown / SetWindowDisplayAffinity Path A call
- app.py: bubble.start_capture() call after construction
- tests/test_window_integration.py: 4 new tests (SWDA, _photo, _image_id,
  worker lifecycle)
- tests/test_capture_smoke.py: 3 Windows-only smoke tests (30 fps, memory
  flat, no hall-of-mirrors)
- Manual verification checkpoint confirming all 7 Phase 3 roadmap criteria
</objective>

<execution_context>
@C:/Users/Jsupport/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Jsupport/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-capture-loop/03-RESEARCH.md
@.planning/phases/03-capture-loop/03-VALIDATION.md
@.planning/phases/03-capture-loop/03-01-SUMMARY.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@src/magnifier_bubble/capture.py
@src/magnifier_bubble/window.py
@src/magnifier_bubble/winconst.py
@src/magnifier_bubble/app.py
@src/magnifier_bubble/state.py
@tests/test_window_integration.py
@tests/conftest.py

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from existing src/. -->
<!-- Executor should use these directly — no codebase exploration needed. -->

From src/magnifier_bubble/capture.py (Plan 03-01 output — FROZEN API):
```python
from typing import Callable
FrameCallback = Callable[["PILImage"], None]

class CaptureWorker(threading.Thread):
    def __init__(
        self,
        state: "AppState",
        on_frame: FrameCallback,
        target_fps: float = 30.0,
    ) -> None: ...
    def stop(self) -> None: ...
    def get_fps(self) -> float: ...
    def run(self) -> None: ...  # inherited from Thread
```

From src/magnifier_bubble/window.py (Phase 2 P03 — existing):
```python
BORDER_COLOR: str = "#2ec4b6"
BORDER_WIDTH: int = 4
STRIP_COLOR: str = "#1a1a1a"
BG_COLOR: str = "#0a0a0a"
DRAG_STRIP_HEIGHT: int = 44
CONTROL_STRIP_HEIGHT: int = 44

class BubbleWindow:
    def __init__(self, state: AppState) -> None:
        # Step 1: tk.Tk() + withdraw
        # Step 2: overrideredirect(True)
        # Step 3: wm_attributes topmost
        # Step 4: geometry
        # Step 5: update_idletasks (realize HWND)
        # Step 6-8: GetParent + ext styles + SetLayeredWindowAttributes
        # (NEW Step 8b inserted here: SetWindowDisplayAffinity)
        # Step 9: tk.Canvas + create_rectangle strips + _draw_border
        # (NEW Step 9b inserted here: _photo + _image_id)
        # Step 10: wndproc.install + install_child x2
        # Step 11: shapes.apply_shape
        # Step 12: deiconify
        # (NEW after Step 12: self._capture_worker = None placeholder)
        self.root: tk.Tk
        self._hwnd: int
        self._canvas: tk.Canvas
        self._top_strip_id: int
        self._bottom_strip_id: int
        self._border_id: int
        self._wndproc_keepalive: ...

    def destroy(self) -> None:
        # Existing: uninstall 3 wndproc chains, then root.destroy()
```

From src/magnifier_bubble/state.py (Phase 1 P02 — existing):
```python
class AppState:
    def capture_region(self) -> tuple[int, int, int, int, float]:
        # returns (x, y, w, h, zoom)
```

From tests/conftest.py (Phase 1 + Phase 2 fixtures):
```python
win_only = pytest.mark.skipif(sys.platform != "win32", reason="Windows-only")

@pytest.fixture(scope="session")
def tk_session_root(): ...

@pytest.fixture
def tk_toplevel(tk_session_root): ...
```

Win32 API (from ctypes + wintypes — already used in window.py):
```python
u32.SetWindowDisplayAffinity.argtypes = [wintypes.HWND, wintypes.DWORD]
u32.SetWindowDisplayAffinity.restype = wintypes.BOOL
# WDA_NONE              = 0x00000000
# WDA_MONITOR           = 0x00000001
# WDA_EXCLUDEFROMCAPTURE= 0x00000011  (Win10 2004+)
u32.GetWindowDisplayAffinity.argtypes = [wintypes.HWND, ctypes.POINTER(wintypes.DWORD)]
u32.GetWindowDisplayAffinity.restype = wintypes.BOOL
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add WDA constants to winconst.py and SetWindowDisplayAffinity call to window.py</name>
  <files>src/magnifier_bubble/winconst.py, src/magnifier_bubble/window.py</files>
  <read_first>
    - src/magnifier_bubble/winconst.py (full — must preserve the "pure
      constants, zero imports beyond __future__" invariant validated by
      test_winconst_body_is_only_constants_and_future_import)
    - src/magnifier_bubble/window.py (full — must preserve the load-bearing
      construction ordering validated by test_source_contains_canonical_ordering)
    - .planning/phases/03-capture-loop/03-RESEARCH.md Pattern 3
      "SetWindowDisplayAffinity" section + Correction 1 (hall-of-mirrors)
    - tests/test_window_integration.py (full — to see which lint tests check
      the ordering; the SWDA call must slot BETWEEN SetLayeredWindowAttributes
      (Step 8) and tk.Canvas(...) (Step 9) so the existing ordering lint still
      passes)
    - tests/test_winconst.py (to see the pure-constants ast lint)
  </read_first>
  <action>
    STEP A — Extend `src/magnifier_bubble/winconst.py` with three new constants
    at the end of the file (after HTBOTTOMRIGHT or in a new section comment
    "# SetWindowDisplayAffinity dwAffinity values"). Preserve the
    "pure constants, no imports, no functions" invariant — add ONLY `= 0x...`
    assignments at module level. Add these three lines verbatim:

        # SetWindowDisplayAffinity dwAffinity values (winuser.h).
        # WDA_EXCLUDEFROMCAPTURE (Win10 2004+) tells the OS to exclude this
        # window from all screen-capture paths — BitBlt+CAPTUREBLT, DXGI
        # Desktop Duplication, screenshot tools, Teams/Zoom screen share.
        # Primary hall-of-mirrors defense (Path A) per 03-RESEARCH.md.
        WDA_NONE               = 0x00000000
        WDA_MONITOR            = 0x00000001
        WDA_EXCLUDEFROMCAPTURE = 0x00000011

    STEP B — Extend `src/magnifier_bubble/window.py`:

    B1. In the existing `_u32()` lazy-binder function body, add argtypes for
        BOTH SetWindowDisplayAffinity (writer) and GetWindowDisplayAffinity
        (reader — used by the integration test to verify affinity was set).
        Insert these lines inside the `if not _WINDOW_SIGNATURES_APPLIED:`
        block, right after the existing `u32.SendMessageW.restype = ...`
        line, BEFORE `_WINDOW_SIGNATURES_APPLIED = True`:

            u32.SetWindowDisplayAffinity.argtypes = [
                wintypes.HWND, wintypes.DWORD
            ]
            u32.SetWindowDisplayAffinity.restype = wintypes.BOOL
            u32.GetWindowDisplayAffinity.argtypes = [
                wintypes.HWND, ctypes.POINTER(wintypes.DWORD)
            ]
            u32.GetWindowDisplayAffinity.restype = wintypes.BOOL

    B2. Insert the Step 8b block AFTER the existing
        `u32.SetLayeredWindowAttributes(self._hwnd, 0, 255, wc.LWA_ALPHA)`
        line (currently the LAST line of the Step 6-8 block at around
        window.py line 130), and BEFORE the `# --- Step 9:` comment.

        EXACT code to insert, indented one level inside the
        `if sys.platform == "win32":` + `if self._hwnd:` block:

            # --- Step 8b (Phase 3): hall-of-mirrors Path A defense ---
            # SetWindowDisplayAffinity(hwnd, WDA_EXCLUDEFROMCAPTURE) tells
            # the OS to exclude this window from all screen-capture paths.
            # Works against BitBlt+CAPTUREBLT (mss 10.1.0), DXGI Desktop
            # Duplication, screenshot tools, and screen sharing. Windows 10
            # 2004+; on older Windows this silently fails and Path B
            # (CAPTUREBLT=0 in capture.py run()) takes over.
            # See .planning/phases/03-capture-loop/03-RESEARCH.md Pattern 3.
            if not u32.SetWindowDisplayAffinity(
                self._hwnd, wc.WDA_EXCLUDEFROMCAPTURE
            ):
                print(
                    "[bubble] SetWindowDisplayAffinity failed "
                    f"(err={ctypes.get_last_error()}); relying on "
                    "capture.py CAPTUREBLT=0 fallback",
                    flush=True,
                )

        The existing `u32 = _u32()` call at the top of the Step 6-8 block
        already binds `u32`, so this call reuses it — do NOT call `_u32()`
        again.

    B3. Do NOT touch the Step 9 canvas creation, the Step 10 wndproc install,
        the Step 11 apply_shape, or the Step 12 deiconify in this task. The
        _photo / _image_id / _on_frame / start_capture additions are Task 2.

    STEP C — Preserve canonical ordering: the existing lint test in
    `tests/test_window_integration.py::test_source_contains_canonical_ordering`
    validates the ORDER that load-bearing substrings appear in window.py
    source text via `src.find()`. SetWindowDisplayAffinity must appear AFTER
    SetLayeredWindowAttributes and BEFORE the `tk.Canvas(` canvas construction
    — this is automatically satisfied by the Step 8b placement above. Do not
    mention the new constants WDA_* in the docstring (docstring is parsed
    first by find() and would shift positions).
  </action>
  <verify>
    <automated>python -m pytest tests/test_winconst.py tests/test_window_integration.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^WDA_EXCLUDEFROMCAPTURE *= *0x00000011$" src/magnifier_bubble/winconst.py` returns 1
    - `grep -c "^WDA_NONE" src/magnifier_bubble/winconst.py` returns 1
    - `grep -c "^WDA_MONITOR" src/magnifier_bubble/winconst.py` returns 1
    - `grep -c "SetWindowDisplayAffinity" src/magnifier_bubble/window.py` returns >= 2 (one argtypes line, one call site)
    - `grep -c "GetWindowDisplayAffinity" src/magnifier_bubble/window.py` returns >= 1 (argtypes line; the actual Get call lives in the test)
    - `grep -c "wc.WDA_EXCLUDEFROMCAPTURE" src/magnifier_bubble/window.py` returns 1
    - `python -m pytest tests/test_winconst.py -x -q` exits 0 (pure-constants invariant still holds — ast lint passes)
    - `python -m pytest tests/test_window_integration.py::test_source_contains_canonical_ordering -x -q` exits 0 (ordering lint still passes — SWDA sits between SetLayeredWindowAttributes and tk.Canvas)
    - `python -m pytest tests/test_window_integration.py -x -q` exits 0 (all existing Phase 2 integration tests still green)
  </acceptance_criteria>
  <done>
    winconst.py exports WDA_NONE, WDA_MONITOR, WDA_EXCLUDEFROMCAPTURE as pure
    constants. window.py calls SetWindowDisplayAffinity(hwnd, WDA_EXCLUDEFROMCAPTURE)
    at Step 8b, with argtypes bound in _u32(). All existing Phase 1 + Phase 2
    tests remain green. Path A hall-of-mirrors defense is in place.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add _photo / _image_id / _on_frame / start_capture to BubbleWindow + update app.py</name>
  <files>src/magnifier_bubble/window.py, src/magnifier_bubble/app.py, tests/test_window_integration.py</files>
  <read_first>
    - src/magnifier_bubble/window.py (full — needs the entire construction
      ordering so additions land in the right place)
    - src/magnifier_bubble/app.py (full — small file, ~50 lines)
    - src/magnifier_bubble/capture.py (output of Plan 03-01 — confirms the
      CaptureWorker signature is `(state, on_frame, target_fps=30.0)`)
    - tests/test_window_integration.py (full — this file will grow by 4 tests;
      must preserve existing fixture `bubble` and the `tk_session_root` usage)
    - .planning/phases/03-capture-loop/03-RESEARCH.md Pattern 2 (single
      PhotoImage + paste) and Example 2 (window.py additions)
    - .planning/research/PITFALLS.md Pitfall 12 (CPython 124364 memory leak)
  </read_first>
  <behavior>
    After this task, window.py exposes:
      - bubble._photo           : ImageTk.PhotoImage
      - bubble._photo_size      : tuple[int, int]  (content_w, content_h)
      - bubble._image_id        : int (canvas item id for the create_image)
      - bubble._capture_worker  : CaptureWorker | None
      - bubble.start_capture()  : starts worker (no-op if already started)
      - bubble._on_frame(img)   : paste into _photo; rebuild on size mismatch
      - bubble.destroy()        : stops worker before root.destroy()

    app.py main() calls `bubble.start_capture()` after construction prints
    and before `mainloop()`. ULTIMATE_ZOOM_SMOKE escape hatch still works.

    Test coverage (new tests in test_window_integration.py):
      - test_set_window_display_affinity: GetWindowDisplayAffinity returns
        0x11 (or 0x01 fallback on older Win)
      - test_photo_attribute_exists: bubble._photo is not None and has the
        expected content-zone width/height
      - test_photo_constructor_uses_positional_mode_size: source text
        contains the exact substring `ImageTk.PhotoImage("RGB", `
      - test_image_id_is_valid_canvas_item: bubble._image_id is in
        bubble._canvas.find_all()
      - test_photoimage_constructed_exactly_twice_in_window: window.py source
        contains exactly 2 occurrences of `ImageTk.PhotoImage(` (one in the
        initial Step 9b build, one in the resize rebuild in _on_frame)
      - test_capture_worker_lifecycle: bubble.start_capture(); bubble._capture_worker
        is not None; bubble._capture_worker.is_alive() returns True; then
        bubble.destroy(); bubble._capture_worker is None

    Existing Phase 2 tests must still pass — do NOT rewrite or delete any
    existing test. Only APPEND new tests to the file.
  </behavior>
  <action>
    STEP A — Modify `src/magnifier_bubble/window.py`:

    A1. Add top-of-file imports (after the existing
        `from magnifier_bubble.state import AppState` line):

            from PIL import ImageTk

        NOTE: do NOT import `from PIL import Image` at top-level — we never
        construct a PIL.Image here; the worker produces them. Importing only
        ImageTk keeps window.py's import graph minimal.

        Also add (after the PIL import):

            from magnifier_bubble.capture import CaptureWorker

        This creates a module-level reference, but CaptureWorker does NOT
        import mss at import time (Plan 03-01 guarantees this), so the Tk
        main thread still never loads mss.

    A2. In `BubbleWindow.__init__`, at the NEW Step 9b (AFTER the
        `self._border_id = self._draw_border(...)` line and the existing
        `self.root.update_idletasks()` Step 9b realize call, BEFORE the
        `# --- Step 10: Install WndProc subclasses ---` comment), insert:

            # --- Step 9b bis (Phase 3): single ImageTk.PhotoImage + canvas image item ---
            # CAPT-05: one PhotoImage, reused every frame via paste() (not
            # reassigned). Rebuilt only on bubble resize via _on_frame's
            # size-mismatch path (Phase 4 will drive that).
            # CPython issue 124364 defense: NEVER create ImageTk.PhotoImage
            # in the hot loop.
            content_w = snap.w
            content_h = snap.h - DRAG_STRIP_HEIGHT - CONTROL_STRIP_HEIGHT
            self._photo: ImageTk.PhotoImage = ImageTk.PhotoImage(
                "RGB", (content_w, content_h)
            )
            self._photo_size: tuple[int, int] = (content_w, content_h)
            # Z-order: image item created LAST among the content-zone items
            # sits ABOVE the canvas background. tag_lower pushes it below the
            # strips and border items so the magnified pixels peek out
            # from the middle zone only.
            self._image_id: int = self._canvas.create_image(
                0, DRAG_STRIP_HEIGHT,
                image=self._photo,
                anchor="nw",
            )
            self._canvas.tag_lower(self._image_id)

    A3. Add `self._capture_worker: CaptureWorker | None = None` at the end
        of __init__, AFTER the existing `self.root.deiconify()` Step 12 line.

    A4. Add two new methods AFTER the existing `_on_canvas_press` method and
        BEFORE the existing `# ---- Public teardown ----` comment:

            def _on_frame(self, img) -> None:
                """Runs on the Tk main thread (via root.after(0, ...)).
                Paste the pre-resized PIL.Image into the single reused
                PhotoImage. Rebuild the PhotoImage only if the bubble has
                been resized since the last frame (Phase 4 will drive this
                path via state.set_size)."""
                if img.size != self._photo_size:
                    self._photo = ImageTk.PhotoImage("RGB", img.size)
                    self._photo_size = img.size
                    self._canvas.itemconfig(self._image_id, image=self._photo)
                self._photo.paste(img)

            def start_capture(self) -> None:
                """Create and start the CaptureWorker producer thread.
                Called from app.main() after BubbleWindow construction
                returns. Safe to call more than once (no-op on re-call)."""
                if self._capture_worker is not None:
                    return
                self._capture_worker = CaptureWorker(
                    state=self.state,
                    on_frame=lambda img: self.root.after(
                        0, self._on_frame, img
                    ),
                )
                self._capture_worker.start()

    A5. In the existing `destroy()` method, add worker teardown as the FIRST
        step inside the `try:` block, BEFORE the existing wndproc uninstall
        chain:

            try:
                # Phase 3: stop the capture worker BEFORE tearing down
                # root / WndProc chain so the worker can't fire one last
                # frame onto a dead canvas.
                if self._capture_worker is not None:
                    self._capture_worker.stop()
                    self._capture_worker.join(timeout=1.0)
                    self._capture_worker = None
                # ... existing wndproc uninstall chain unchanged ...
                if self._canvas_wndproc_keepalive is not None:
                    ...

    STEP B — Modify `src/magnifier_bubble/app.py`:

    In `main()`, AFTER the existing `print(f"[bubble] hwnd=...)` line and
    BEFORE the `if os.environ.get("ULTIMATE_ZOOM_SMOKE") == "1":` block, add:

        # Phase 3: start the 30 fps capture producer thread.
        # (Does nothing on non-Windows because CaptureWorker.run()'s
        # `import mss` will fail — we gate by sys.platform to avoid a
        # confusing ModuleNotFoundError on CI / non-Windows dev machines.)
        import sys as _sys
        if _sys.platform == "win32":
            bubble.start_capture()

    STEP C — Append new tests to `tests/test_window_integration.py`.
    ONLY APPEND — do not rewrite any existing test. Add these 6 tests at
    the end of the file, all using the existing `bubble` fixture (or
    create a new `bubble` fixture that is identical to the Phase 2 one if
    no such fixture exists — inspect the file first; Phase 2 P03 created
    a `bubble` fixture that yields a BubbleWindow and tears it down).

    Required new tests (paste verbatim into the file):

        # --- Phase 3 additions ---
        @pytest.mark.skipif(sys.platform != "win32", reason="Windows-only")
        def test_set_window_display_affinity(bubble):
            """CAPT-06 Path A: SetWindowDisplayAffinity was called."""
            from ctypes import wintypes, byref
            u32 = ctypes.windll.user32  # type: ignore[attr-defined]
            u32.GetWindowDisplayAffinity.argtypes = [
                wintypes.HWND, ctypes.POINTER(wintypes.DWORD)
            ]
            u32.GetWindowDisplayAffinity.restype = wintypes.BOOL
            affinity = wintypes.DWORD()
            assert u32.GetWindowDisplayAffinity(
                wintypes.HWND(bubble._hwnd), byref(affinity)
            )
            # 0x11 = WDA_EXCLUDEFROMCAPTURE (Win10 2004+), 0x01 = WDA_MONITOR
            # fallback on older Windows. 0x00 = WDA_NONE = call failed.
            assert affinity.value in (0x01, 0x11), (
                f"unexpected display affinity: {affinity.value:#x}"
            )

        @pytest.mark.skipif(sys.platform != "win32", reason="Windows-only")
        def test_photo_attribute_exists(bubble):
            """CAPT-05: BubbleWindow has a single ImageTk.PhotoImage."""
            from PIL import ImageTk
            assert isinstance(bubble._photo, ImageTk.PhotoImage)
            assert bubble._photo_size == (
                bubble._photo.width(), bubble._photo.height()
            )
            # Content zone = bubble - top strip - bottom strip
            from magnifier_bubble.window import (
                DRAG_STRIP_HEIGHT, CONTROL_STRIP_HEIGHT,
            )
            snap = bubble.state.snapshot()
            assert bubble._photo.width() == snap.w
            assert bubble._photo.height() == (
                snap.h - DRAG_STRIP_HEIGHT - CONTROL_STRIP_HEIGHT
            )

        @pytest.mark.skipif(sys.platform != "win32", reason="Windows-only")
        def test_image_id_is_valid_canvas_item(bubble):
            """CAPT-04 wiring: _image_id is a live Canvas item id."""
            assert bubble._image_id in bubble._canvas.find_all()
            # Item type is "image" (not rectangle, oval, etc.)
            assert bubble._canvas.type(bubble._image_id) == "image"

        def test_photoimage_constructed_exactly_twice_in_window():
            """CAPT-05 Pitfall 12 lint: only two ImageTk.PhotoImage(
            call sites in window.py (initial build + resize rebuild)."""
            src = pathlib.Path(window_mod.__file__).read_text(encoding="utf-8")
            count = src.count("ImageTk.PhotoImage(")
            assert count == 2, (
                f"ImageTk.PhotoImage( appears {count} times in window.py; "
                f"expected exactly 2 (initial build in __init__ + resize "
                f"rebuild in _on_frame). More than 2 means CPython 124364 "
                f"leak defense is broken."
            )

        def test_on_frame_uses_paste_not_reassign():
            """CAPT-05 lint: _on_frame must use self._photo.paste(img)."""
            src = pathlib.Path(window_mod.__file__).read_text(encoding="utf-8")
            assert "self._photo.paste(" in src, (
                "_on_frame must call self._photo.paste(img) per CAPT-05"
            )

        @pytest.mark.skipif(sys.platform != "win32", reason="Windows-only")
        def test_capture_worker_lifecycle(bubble):
            """CAPT-01: start_capture creates a live worker; destroy stops it."""
            import time
            assert bubble._capture_worker is None
            bubble.start_capture()
            assert bubble._capture_worker is not None
            assert bubble._capture_worker.is_alive()
            # Calling start twice is a no-op
            first = bubble._capture_worker
            bubble.start_capture()
            assert bubble._capture_worker is first
            # Let one frame try to arrive
            bubble.root.update()
            time.sleep(0.05)
            bubble.root.update()
            # destroy() must stop the worker
            bubble.destroy()
            assert bubble._capture_worker is None

    NOTES for test appending:
    - If `bubble` fixture does NOT exist in the current test_window_integration.py,
      replicate the Phase 2 pattern: construct `AppState(StateSnapshot())`,
      construct `BubbleWindow(state)`, yield it, then call `destroy()` in
      teardown — AND wrap in a try/except TclError for cleanup safety.
    - The `test_capture_worker_lifecycle` test calls `bubble.destroy()` INSIDE
      the test body, so it must be the LAST test in its own fixture scope,
      OR use a dedicated `bubble` instance that the shared fixture is not
      reused after destruction. Prefer a per-test bubble for this one test
      to avoid fixture teardown conflicts.
  </action>
  <verify>
    <automated>python -m pytest tests/test_window_integration.py tests/test_capture.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^from PIL import ImageTk$" src/magnifier_bubble/window.py` returns 1
    - `grep -c "from magnifier_bubble.capture import CaptureWorker" src/magnifier_bubble/window.py` returns 1
    - `grep -c "self\._photo" src/magnifier_bubble/window.py` returns >= 4 (init + _on_frame + destroy gate + photo_size)
    - `grep -c "ImageTk.PhotoImage(" src/magnifier_bubble/window.py` returns exactly 2
    - `grep -c "self\._photo\.paste(" src/magnifier_bubble/window.py` returns 1
    - `grep -c "self\._image_id" src/magnifier_bubble/window.py` returns >= 3
    - `grep -c "self\._canvas\.create_image(" src/magnifier_bubble/window.py` returns 1
    - `grep -c "def start_capture" src/magnifier_bubble/window.py` returns 1
    - `grep -c "def _on_frame" src/magnifier_bubble/window.py` returns 1
    - `grep -c "self\._capture_worker\.stop()" src/magnifier_bubble/window.py` returns 1
    - `grep -c "self\._capture_worker\.join" src/magnifier_bubble/window.py` returns 1
    - `grep -c "bubble\.start_capture()" src/magnifier_bubble/app.py` returns 1
    - `grep -c "def test_set_window_display_affinity" tests/test_window_integration.py` returns 1
    - `grep -c "def test_photo_attribute_exists" tests/test_window_integration.py` returns 1
    - `grep -c "def test_image_id_is_valid_canvas_item" tests/test_window_integration.py` returns 1
    - `grep -c "def test_photoimage_constructed_exactly_twice_in_window" tests/test_window_integration.py` returns 1
    - `grep -c "def test_capture_worker_lifecycle" tests/test_window_integration.py` returns 1
    - `python -m pytest tests/test_window_integration.py -x -q` exits 0 on the Windows dev box
    - `python -m pytest tests/ -x -q` exits 0 (no Phase 1/2 regressions)
    - `python -c "from magnifier_bubble.window import BubbleWindow; import inspect; assert 'start_capture' in dir(BubbleWindow)"` exits 0
  </acceptance_criteria>
  <done>
    window.py exposes _photo / _image_id / _on_frame / start_capture / destroy-
    with-worker-teardown. app.py starts the worker on Windows after the bubble
    is constructed. 6 new tests in test_window_integration.py cover the added
    surface. All existing Phase 1 + Phase 2 + Plan 03-01 tests still green.
    CAPT-05 and the Path A side of CAPT-06 are code-verified. The app is now
    CAPABLE of showing live capture, but we still need the Windows-only
    smoke tests (Task 3) and manual verification (Task 4) to prove it works
    on real hardware.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create tests/test_capture_smoke.py (Windows-only fps/memory/hall-of-mirrors tests)</name>
  <files>tests/test_capture_smoke.py</files>
  <read_first>
    - tests/conftest.py (`win_only` marker and `tk_session_root` fixture;
      note that tk_session_root destroys the root at session end — tests
      that create a BubbleWindow must NOT reuse tk_session_root because
      BubbleWindow creates its own `tk.Tk()` in Step 1, which conflicts
      with the session root; see the Phase 2 pattern in
      test_window_integration.py where `bubble` fixture creates a standalone
      BubbleWindow, not a Toplevel off tk_session_root)
    - tests/test_window_integration.py (reference for how the existing
      `bubble` fixture or per-test BubbleWindow construction is done)
    - .planning/phases/03-capture-loop/03-RESEARCH.md Example 4
      "integration test for fps + memory + hall-of-mirrors" (lines 889-1001)
    - src/magnifier_bubble/window.py (the BORDER_COLOR constant #2ec4b6 is
      what the hall-of-mirrors test asserts is NOT present)
  </read_first>
  <action>
    Create `tests/test_capture_smoke.py` as a new file. Windows-only. Imports
    mss + PIL + BubbleWindow. Must skip gracefully on non-Windows.

    Required test functions (verbatim structure from 03-RESEARCH.md Example 4,
    adapted for the project's conftest.py and the destroy() contract):

        \"\"\"Phase 3 capture-loop smoke tests — Windows-only.

        Runs the full pipeline:
          BubbleWindow __init__ -> start_capture -> worker.run() -> mss.grab
          -> BILINEAR resize -> root.after(0, _on_frame, img) -> _photo.paste

        Verifies CAPT-02 (>= 30 fps over 2s), CAPT-05 (< 5 MB drift over 60s),
        CAPT-06 (no hall-of-mirrors in a captured frame).

        These tests require a live display and will not run on headless CI.
        \"\"\"
        from __future__ import annotations

        import sys
        import time
        import pathlib
        import pytest
        import tracemalloc

        pytestmark = pytest.mark.skipif(
            sys.platform != "win32",
            reason="Windows-only — requires live display + mss + pywin32"
        )


        def _make_bubble(w: int = 400, h: int = 400, zoom: float = 2.0):
            \"\"\"Construct a BubbleWindow at a known geometry for a test.
            Caller is responsible for calling bubble.destroy() in a finally.\"\"\"
            from magnifier_bubble.state import AppState, StateSnapshot
            from magnifier_bubble.window import BubbleWindow
            state = AppState(StateSnapshot(x=200, y=200, w=w, h=h, zoom=zoom))
            return BubbleWindow(state)


        def _drain_tk(bubble, duration_s: float) -> None:
            \"\"\"Run bubble.root.update() for roughly duration_s seconds so
            the Tk event loop processes the root.after(0, ...) callbacks
            queued by the capture worker.\"\"\"
            deadline = time.perf_counter() + duration_s
            while time.perf_counter() < deadline:
                bubble.root.update()
                time.sleep(0.01)


        def test_capture_worker_starts_and_frames_arrive():
            \"\"\"CAPT-01: start_capture triggers at least one frame.\"\"\"
            bubble = _make_bubble()
            frames_received = []
            original = bubble._on_frame
            def spy(img):
                frames_received.append(img.size)
                original(img)
            bubble._on_frame = spy
            try:
                bubble.start_capture()
                _drain_tk(bubble, 1.0)
                assert frames_received, (
                    "no frames arrived within 1s of start_capture"
                )
                assert all(
                    sz[0] > 0 and sz[1] > 0 for sz in frames_received
                ), "frames have zero dimensions"
            finally:
                bubble.destroy()


        def test_capture_worker_achieves_30fps():
            \"\"\"CAPT-02: sustained >= 30 fps over 2 seconds.\"\"\"
            bubble = _make_bubble(w=400, h=400, zoom=2.0)
            try:
                bubble.start_capture()
                _drain_tk(bubble, 2.0)
                assert bubble._capture_worker is not None
                fps = bubble._capture_worker.get_fps()
                assert fps >= 30.0, (
                    f"CAPT-02 FAILED: fps was {fps:.1f}, expected >= 30.0"
                )
            finally:
                bubble.destroy()


        def test_capture_memory_flat_over_60s():
            \"\"\"CAPT-05 Pitfall 12: memory drift < 5 MB over 60 seconds.\"\"\"
            bubble = _make_bubble(w=400, h=400, zoom=2.0)
            try:
                bubble.start_capture()
                # Warm-up: let mss + PhotoImage allocate steady-state memory
                _drain_tk(bubble, 5.0)

                tracemalloc.start()
                snap_before = tracemalloc.take_snapshot()

                _drain_tk(bubble, 60.0)

                snap_after = tracemalloc.take_snapshot()
                tracemalloc.stop()

                stats = snap_after.compare_to(snap_before, "filename")
                total_drift = sum(s.size_diff for s in stats)
                # < 5 MB per REQUIREMENTS.md CAPT-05 / ROADMAP Phase 3 #4
                assert total_drift < 5 * 1024 * 1024, (
                    f"CAPT-05 FAILED: memory drift "
                    f"{total_drift / 1024 / 1024:.2f} MB exceeds 5 MB"
                )
            finally:
                bubble.destroy()


        def test_no_hall_of_mirrors():
            \"\"\"CAPT-06: the bubble's own teal border must not appear in
            a captured frame (SetWindowDisplayAffinity + CAPTUREBLT=0).\"\"\"
            from magnifier_bubble.window import BORDER_COLOR
            bubble = _make_bubble(w=400, h=400, zoom=2.0)
            captured = []
            original = bubble._on_frame

            def spy(img):
                if not captured:
                    captured.append(img.copy())
                original(img)

            bubble._on_frame = spy
            try:
                bubble.start_capture()
                _drain_tk(bubble, 1.0)
                assert captured, "no frame captured in 1s"
                img = captured[0]
                # teal_rgb = (46, 196, 182)  -- #2ec4b6 decoded
                teal_rgb = tuple(
                    int(BORDER_COLOR.lstrip("#")[i:i + 2], 16)
                    for i in (0, 2, 4)
                )
                assert teal_rgb == (46, 196, 182), (
                    f"BORDER_COLOR decoded to {teal_rgb}, "
                    f"expected (46, 196, 182)"
                )
                # Sample a 10x10 grid across the image; none should equal
                # the exact teal. If the bubble were leaking into its own
                # capture, the top/bottom rows would be full of teal pixels
                # (the border runs around the whole bubble).
                w_img, h_img = img.size
                teal_hits = 0
                for gx in range(10):
                    for gy in range(10):
                        px = img.getpixel((
                            gx * (w_img - 1) // 9,
                            gy * (h_img - 1) // 9,
                        ))
                        if px[:3] == teal_rgb:
                            teal_hits += 1
                assert teal_hits == 0, (
                    f"CAPT-06 FAILED: {teal_hits}/100 sampled pixels "
                    f"match bubble teal border — hall-of-mirrors detected"
                )
            finally:
                bubble.destroy()

    Notes:
    - `pytestmark` at module level skips every test on non-Windows (no need
      to decorate each function individually).
    - The memory test uses `tracemalloc` to measure Python-allocation drift,
      not RSS. If _photo is leaking Tk C-side memory, tracemalloc will still
      catch the Python wrapper allocations. The ROADMAP criterion is "< 5 MB"
      drift, which is met for ~60 s at any reasonable leak rate if the paste()
      pattern is working.
    - The hall-of-mirrors test uses a 10×10 sampling grid rather than a
      single pixel check — more robust against anti-aliasing edge cases while
      still catching a real feedback loop (which paints teal pixels across
      many rows of the image).
    - NO tkinter.Tk() creation outside BubbleWindow — the project's
      `_make_bubble` helper creates its own root via BubbleWindow.__init__
      Step 1. The session-scoped `tk_session_root` fixture is NOT used here.
  </action>
  <verify>
    <automated>python -m pytest tests/test_capture_smoke.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/test_capture_smoke.py` exists
    - `grep -c "def test_capture_worker_starts_and_frames_arrive" tests/test_capture_smoke.py` returns 1
    - `grep -c "def test_capture_worker_achieves_30fps" tests/test_capture_smoke.py` returns 1
    - `grep -c "def test_capture_memory_flat_over_60s" tests/test_capture_smoke.py` returns 1
    - `grep -c "def test_no_hall_of_mirrors" tests/test_capture_smoke.py` returns 1
    - `grep -c 'pytestmark *= *pytest.mark.skipif' tests/test_capture_smoke.py` returns 1
    - `grep -c "tracemalloc" tests/test_capture_smoke.py` returns >= 2 (start + take_snapshot)
    - `grep -c "BORDER_COLOR" tests/test_capture_smoke.py` returns >= 1
    - `python -m pytest tests/test_capture_smoke.py --collect-only -q` lists 4 test functions (collect works even if skipping later)
    - `python -m pytest tests/test_capture_smoke.py -x -q` exits 0 on the Windows dev box (all 4 tests pass — or skip cleanly on non-Windows with `1 skipped` or `4 skipped`)
    - `python -m pytest tests/ -x -q` exits 0 overall (no regressions)
    - The 30 fps test reports actual fps in the assertion message on failure
    - The memory test reports actual MB drift in the assertion message on failure
    - The hall-of-mirrors test reports how many of 100 sampled pixels matched the teal border on failure
  </acceptance_criteria>
  <done>
    tests/test_capture_smoke.py exists with 4 Windows-only smoke tests that
    exercise the full BubbleWindow + CaptureWorker pipeline. On the dev box
    all 4 pass. CAPT-01, CAPT-02, CAPT-05, CAPT-06 are now automated-verified
    (CAPT-03 lint + CAPT-04 BILINEAR literal were already green from Plan 03-01).
    Phase 3's automated validation gate is complete — the only remaining
    verification is human-eye + multi-minute soak (Task 4).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: 7-point manual verification of Phase 3 on Windows dev box</name>
  <files>N/A (verification only — no file modifications; observes the running app from Tasks 1-3)</files>
  <read_first>
    - .planning/ROADMAP.md Phase 3 Success Criteria (the 5 observable truths
      this checkpoint is proving)
    - .planning/REQUIREMENTS.md CAPT-01 through CAPT-06 (the 6 requirements
      this checkpoint validates on real hardware)
  </read_first>
  <action>
    DO NOT modify any files in this task. This is a pure human-in-the-loop
    verification checkpoint. Your job as the executor is to:

    1. Run the full automated suite and confirm it is green:
       `python -m pytest tests/ -v`
       Paste the tail of the output into the plan SUMMARY.

    2. Display the <what-built> block (below) to the user so they understand
       what Phase 3 delivered.

    3. Display the <how-to-verify> block (below) to the user as a 7-step
       checklist with specific expected behaviors.

    4. Launch the running app in a background shell:
       `python main.py`
       (Do NOT set ULTIMATE_ZOOM_SMOKE=1 — this is an interactive session.)

    5. Pause and wait for the user to complete all 7 manual checks and
       return a resume signal. DO NOT proceed until you get either
       "approved" or a description of the failed check(s).

    6. If the user reports a failure, record the exact failure description
       in the plan SUMMARY under a "Manual Verification Failures" heading
       and exit the plan WITHOUT marking it complete — the user will then
       run `/gsd:plan-phase 03 --gaps` to generate a gap-closure plan.

    7. If the user approves, record all 7 checks as PASSED in the plan
       SUMMARY and proceed to the final commit.
  </action>
  <what-built>
    Phase 3 is code-complete after Tasks 1-3:
    - capture.py (Plan 03-01) — 30 fps producer thread with mss + BILINEAR
      + single-instance PhotoImage paste pattern + outer reconnect loop
    - winconst.py — WDA_EXCLUDEFROMCAPTURE constant added
    - window.py — Step 8b SetWindowDisplayAffinity Path A, Step 9b single
      ImageTk.PhotoImage + canvas create_image item, _on_frame paste handler,
      start_capture() wiring, destroy() worker teardown
    - app.py — bubble.start_capture() after construction on Windows
    - tests/test_capture.py — 13+ structural/lint tests (pure Python)
    - tests/test_capture_smoke.py — 4 Windows-only fps/memory/hall-of-mirrors tests
    - tests/test_window_integration.py — 6 new tests covering Phase 3 surface
      on top of the existing Phase 2 integration tests

    The automated suite asserts:
      - fps >= 30 over a 2-second window (CAPT-02)
      - memory drift < 5 MB over a 60-second window (CAPT-05)
      - no teal pixels in the captured frame (CAPT-06 Path A proof)
      - structural lint: no ImageGrab anywhere in src/, BILINEAR literal
        exactly once in capture.py, mss.mss() only inside run(), etc.

    What the automated suite CANNOT prove (requires human eyes + real apps):
      - Moving another app (Notepad) under the bubble shows ITS content
        magnified — not the desktop wallpaper, not the previous frame, not
        garbage pixels
      - The 30 fps target is met AT THE VISUAL LEVEL (smooth motion when
        a video plays underneath) — the fps counter could pass while the
        frame being rendered is stale
      - Phase 2 Pattern 2b drag still works — dragging the top strip moves
        the bubble smoothly and the magnified content tracks the new position
      - Phase 2 content-zone click-through is still intact — clicks in the
        middle pass through to Notepad
      - The bubble does NOT steal focus from Notepad while capturing
      - The bubble does NOT mirror itself (roadmap criterion 3) — dragging
        the bubble over its previous screen location does not produce the
        hall-of-mirrors effect
      - 10-minute memory stability (ROADMAP criterion 4 says 10 minutes; the
        automated test only runs 60 seconds due to CI time budget)
  </what-built>
  <how-to-verify>
    Run these 7 checks in order on the Windows dev box. Have Notepad or any
    text editor open behind the bubble as the "background to be magnified."

    **Setup:**
    1. Close any previous runs of Ultimate Zoom (check Task Manager for
       `python.exe` with `ultimate` or `magnifier` in the args if unsure).
    2. Ensure venv is active and deps are installed:
       `python -c "import mss, PIL; print('ok')"`
    3. Open Notepad. Type/paste some readable text ("Hello Phase 3 capture!").
    4. Launch the bubble: `python main.py` (or `python -m magnifier_bubble`).
       Do NOT set ULTIMATE_ZOOM_SMOKE.

    **Check 1 — Live magnified pixels visible (CAPT-01, CAPT-04):**
    Expected: the middle content zone of the bubble shows a magnified (~2x)
    view of whatever is under it — probably the desktop wallpaper. Move the
    bubble over Notepad by dragging the top strip — Notepad's text should
    become visible in the bubble, magnified, legibly readable.
    PASS criterion: you can READ the magnified text through the bubble.
    FAIL signs: bubble is black, bubble shows garbage pixels, bubble shows
    stale frozen content, bubble shows its own teal border recursively.

    **Check 2 — 30 fps smooth motion (CAPT-02):**
    Expected: with the bubble over Notepad, open a video or GIF in another
    window (or scroll rapidly in Notepad using Page Down). The magnified
    view should update smoothly with no visible stutter. If you have a video
    playing underneath, the magnified video should play smoothly at the
    same visual framerate as the original.
    PASS criterion: no visible stutter when source content updates rapidly.
    FAIL signs: magnified view updates in chunks, lags behind, or freezes.

    **Check 3 — Hall-of-mirrors absence (CAPT-06):**
    Expected: drag the bubble across its OWN previous screen position. The
    bubble content should NEVER show a shrunken copy of itself inside. No
    teal rectangle should appear inside the magnified view at any position.
    PASS criterion: dragging over the bubble's own former location shows
    ONLY the apps underneath — no self-reflection.
    FAIL signs: teal pixels visible inside the content zone, infinite-mirror
    effect, tiny rectangles visible.

    **Check 4 — Phase 2 drag still works (LAYT-01, Pattern 2b regression test):**
    Expected: grab the top strip of the bubble and drag it around the screen.
    The bubble should move smoothly with the cursor. The drag should NOT
    freeze, jitter, or cause the magnified content to go black.
    PASS criterion: bubble drags smoothly; magnified content keeps updating
    as the bubble moves (you should see different content as the bubble
    traverses different underlying windows).
    FAIL signs: drag is choppy or frozen, content goes black during drag.

    **Check 5 — Phase 2 content-zone click-through still works (LAYT-02):**
    Expected: click in the MIDDLE of the bubble (the magnified content zone).
    The click should pass through to Notepad underneath, moving its caret
    or selecting text. The bubble should NOT consume the click.
    PASS criterion: clicks in the content zone land on Notepad — test by
    clicking in Notepad's text area; Notepad's caret should move where you
    clicked.
    FAIL signs: click is eaten by the bubble; Notepad caret doesn't move;
    focus switches to the bubble.

    **Check 6 — No focus theft (OVER-04 regression test):**
    Expected: click inside Notepad, start typing. Bubble is visible and
    capturing. Click on the bubble's top strip (drag handle, which per
    Phase 2 Pattern 2b does start a drag). RELEASE without moving.
    Click back in Notepad and type again. Notepad should still receive
    the typing — the bubble should NOT have stolen keyboard focus.
    PASS criterion: keyboard focus stays with Notepad throughout.
    FAIL signs: after clicking the bubble, typing doesn't reach Notepad.

    **Check 7 — 10-minute memory soak (CAPT-05, ROADMAP criterion 4):**
    Open Windows Task Manager -> Details tab -> find `python.exe` (the one
    running `main.py`) -> add "Memory (private working set)" column. Note
    the starting value. Let the app run for 10 minutes with the bubble
    over Notepad / a video / anything changing. At the end, note the final
    value.
    PASS criterion: memory grew by LESS than 5 MB (the ROADMAP threshold).
    Ideal: < 1 MB drift. The paste() pattern should keep memory essentially
    flat.
    FAIL signs: memory grew by > 5 MB (hints at the CPython 124364 leak
    being reintroduced somewhere); memory grew by > 50 MB over 10 min
    (catastrophic leak — check that ImageTk.PhotoImage is not being
    constructed per frame).

    **Cleanup:** Close the bubble by its window-close handler (or by killing
    python.exe if the Phase 4 close button doesn't exist yet). Confirm the
    process exits cleanly (no orphaned python.exe in Task Manager).
  </how-to-verify>
  <verify>
    <automated>python -m pytest tests/ -v</automated>
    Manual checks 1-7 run by the user on the Windows dev box with the
    app executing via `python main.py`. No automated command can replace
    visual confirmation of the live magnification / smooth motion / no
    hall-of-mirrors / drag / click-through / focus / 10-min memory stability.
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/ -v` exits 0 with ZERO failures and ZERO
      errors before the user begins manual verification
    - `python main.py` launches without errors on the dev box and produces
      a visible bubble on screen
    - User reports Check 1 (live magnified pixels) as PASS (can read
      magnified Notepad text through the bubble)
    - User reports Check 2 (30 fps smooth motion) as PASS (no visible
      stutter when source content updates rapidly)
    - User reports Check 3 (hall-of-mirrors absence) as PASS (no teal
      rectangles inside the content zone when dragged over previous
      bubble position)
    - User reports Check 4 (Phase 2 drag regression) as PASS (bubble drags
      smoothly via top strip, no black screen during drag)
    - User reports Check 5 (Phase 2 click-through regression) as PASS
      (clicks in the content zone land on Notepad caret underneath)
    - User reports Check 6 (Phase 2 focus regression) as PASS (keyboard
      focus stays with Notepad after clicking bubble)
    - User reports Check 7 (10-minute memory soak) as PASS (memory drift
      observed in Task Manager is < 5 MB after 10 minutes)
    - Final resume signal from user is exactly "approved" (lowercase)
  </acceptance_criteria>
  <done>
    All 7 manual checks returned PASS. User typed "approved". The plan
    SUMMARY records the observed fps, memory drift, and SWDA affinity
    value. Phase 3 is complete — Ultimate Zoom now shows live magnified
    pixels on screen and meets every ROADMAP Phase 3 success criterion.

    If the user reported a failure on ANY check, the plan is NOT done;
    record the failure in SUMMARY and stop. Next step is
    `/gsd:plan-phase 03 --gaps` to generate a gap-closure plan.
  </done>
  <resume-signal>
    Type "approved" if all 7 checks pass, OR describe the specific check(s)
    that failed with observable details (what you saw, what you expected,
    which check number). Gap closure can then be driven by
    `/gsd:plan-phase 03 --gaps` based on the failures.
  </resume-signal>
</task>

</tasks>

<verification>
Automated verification BEFORE the manual checkpoint:

  python -m pytest tests/ -v

Specifically the Phase 3 Plan 02 gates:
  python -m pytest tests/test_window_integration.py -x
  python -m pytest tests/test_capture.py -x
  python -m pytest tests/test_capture_smoke.py -x

All three must exit 0 on the Windows dev box. The smoke tests will skip
cleanly on non-Windows CI and run fully on the dev box.

Also run the lint gates:
  grep -r "ImageGrab" src/magnifier_bubble/ || echo "CAPT-03 OK: zero matches"
  grep -c "ImageTk.PhotoImage(" src/magnifier_bubble/window.py   # expect 2
  grep -c "Resampling.BILINEAR" src/magnifier_bubble/capture.py  # expect 1

AFTER the manual checkpoint:
- All 7 manual checks pass, OR
- User describes which check(s) failed; Phase 3 is then in gap-closure mode
  via `/gsd:plan-phase 03 --gaps`.
</verification>

<success_criteria>
1. All 4 tests in `tests/test_capture_smoke.py` pass on the Windows dev box
   (test_capture_worker_starts_and_frames_arrive, _achieves_30fps,
   _memory_flat_over_60s, _no_hall_of_mirrors)
2. All 6 new tests in `tests/test_window_integration.py` pass on the dev
   box (test_set_window_display_affinity, test_photo_attribute_exists,
   test_image_id_is_valid_canvas_item,
   test_photoimage_constructed_exactly_twice_in_window,
   test_on_frame_uses_paste_not_reassign, test_capture_worker_lifecycle)
3. `python -m pytest tests/ -x -q` exits 0 (all Phase 1 + Phase 2 + Phase 3
   tests green; no regressions)
4. `grep -r "ImageGrab" src/magnifier_bubble/` returns zero matches (CAPT-03)
5. `grep -c "ImageTk.PhotoImage(" src/magnifier_bubble/window.py` returns 2
   (CAPT-05 Pitfall 12: initial build + resize rebuild ONLY)
6. `grep -c "Resampling.BILINEAR" src/magnifier_bubble/capture.py` returns 1
   (CAPT-04)
7. Running `python main.py` shows a bubble with live magnified content
8. All 7 manual checks pass, confirming:
   - Live magnification (CAPT-01, CAPT-04) — Check 1
   - Visual 30 fps smoothness (CAPT-02) — Check 2
   - No hall-of-mirrors (CAPT-06) — Check 3
   - Phase 2 drag regression (LAYT-01, Pattern 2b) — Check 4
   - Phase 2 click-through regression (LAYT-02) — Check 5
   - Phase 2 focus regression (OVER-04) — Check 6
   - 10-minute memory stability (CAPT-05) — Check 7
9. Phase 3 ROADMAP criteria 1, 2, 3, 4, 5 are all observable as TRUE on
   the Windows dev box after this plan
</success_criteria>

<output>
After completion, create:
`.planning/phases/03-capture-loop/03-02-SUMMARY.md`

Include in the summary:
- The pytest output for tests/test_capture_smoke.py -v (full table of 4
  results with times)
- The actual fps measured (from test_capture_worker_achieves_30fps)
- The actual memory drift in MB (from test_capture_memory_flat_over_60s)
- The actual teal_hits count (from test_no_hall_of_mirrors) — should be 0
- Which hall-of-mirrors defense path provided the protection:
  run `python -c "from magnifier_bubble.state import AppState, StateSnapshot; from magnifier_bubble.window import BubbleWindow; from ctypes import wintypes, byref; import ctypes; b = BubbleWindow(AppState(StateSnapshot())); u = ctypes.windll.user32; u.GetWindowDisplayAffinity.argtypes = [wintypes.HWND, ctypes.POINTER(wintypes.DWORD)]; u.GetWindowDisplayAffinity.restype = wintypes.BOOL; a = wintypes.DWORD(); u.GetWindowDisplayAffinity(wintypes.HWND(b._hwnd), byref(a)); print(hex(a.value)); b.destroy()"`
  and record the hex value (expect 0x11 = Path A active, or 0x01 fallback).
- The results of the 7-point manual verification (PASS/FAIL per check + notes)
- The observed 10-minute memory drift in MB (Task Manager)
- Any Rule 1 bugs discovered and their fixes
- Confirmation that Phase 2's drag, click-through, and focus behaviors are
  unaffected
- Update STATE.md decisions with Phase 3 findings (SWDA path used, any
  fallback triggers, any 10-minute drift observations)
</output>
