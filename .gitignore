# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.pytest_cache/

# Virtual environments
.venv/
venv/
env/

# PyInstaller (Phase 8)
build/
dist/
*.spec.bak

# Runtime state (Phase 5)
config.json

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp

# Agent scaffolding (internal tooling — not for public repo)
.claude/
.remember/

# Debug log (contains runtime exception traces — not for public repo)
zoom_log.txt

# Runtime theme state (written by app at runtime — not a source file)
theme.json

# Security — never commit these
github-recovery-codes.txt

# Atomic-write staging files — must never be shipped or committed
*.tmp
