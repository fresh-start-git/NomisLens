---
phase: 03-capture-loop
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/magnifier_bubble/capture.py
  - tests/test_capture.py
  - requirements.txt
  - requirements-dev.txt
autonomous: true
requirements: [CAPT-01, CAPT-02, CAPT-03, CAPT-04]
must_haves:
  truths:
    - "mss 10.1.0 and Pillow wheels install cleanly on the dev box Python interpreter"
    - "src/magnifier_bubble/capture.py defines class CaptureWorker(threading.Thread) with start/stop/get_fps"
    - "CaptureWorker.run() constructs mss.mss() inside its own body (thread-local safety)"
    - "CaptureWorker performs BILINEAR resize on the worker thread before handing the PIL.Image to on_frame"
    - "CaptureWorker uses threading.Event.wait() everywhere inside run() — no time.sleep() Call node exists anywhere inside the run() function AST"
    - "CaptureWorker catches mss GetDIBits failures and reconnects via an outer while-loop"
    - "grep -r ImageGrab src/magnifier_bubble/ returns zero matches"
    - "python -m pytest tests/test_capture.py -x exits 0 with all 13+ tests passing (externally-observable command anchor)"
    - "python -m pytest tests/ -x -q exits 0 with zero regressions in Phase 1/2 tests (externally-observable command anchor)"
    - "python -c 'from magnifier_bubble import capture; print(capture.CaptureWorker.__mro__[1].__name__)' prints 'Thread' (externally-observable import smoke-check)"
  artifacts:
    - path: "src/magnifier_bubble/capture.py"
      provides: "CaptureWorker(threading.Thread, daemon=True) with run(), stop(), get_fps(), _tick()"
      min_lines: 80
      contains: "class CaptureWorker"
    - path: "tests/test_capture.py"
      provides: "Pure-Python lint + unit tests for CaptureWorker structure"
      min_lines: 120
      contains: "def test_no_imagegrab_in_capture_source"
    - path: "requirements.txt"
      provides: "mss pin added (or confirmed present)"
      contains: "mss=="
    - path: "requirements-dev.txt"
      provides: "Development dependencies (superset of runtime)"
  key_links:
    - from: "src/magnifier_bubble/capture.py"
      to: "src/magnifier_bubble/state.py"
      via: "state.capture_region() call inside CaptureWorker._tick()"
      pattern: "state\\.capture_region\\("
    - from: "src/magnifier_bubble/capture.py"
      to: "mss.mss()"
      via: "lazy import inside run() method (NOT __init__)"
      pattern: "def run.*?import mss"
    - from: "src/magnifier_bubble/capture.py"
      to: "PIL.Image.Resampling.BILINEAR"
      via: "img.resize((w, h), Image.Resampling.BILINEAR) call"
      pattern: "Resampling\\.BILINEAR"
---

<objective>
Create the Phase 3 `capture.py` module — the pure-Python 30 fps producer thread
that reads coordinates from AppState, grabs a screen region via mss, BILINEAR-
resizes with Pillow, and hands the resulting PIL.Image to an on_frame callback.
Plus create the pure-Python test scaffolding (tests/test_capture.py) that lint-
verifies the file structure, BILINEAR usage, absence of ImageGrab, mss thread-
local safety, and frame-pacing correctness.

Purpose: Phase 3's capture worker is the biggest new component in the phase and
has sharp thread-safety, memory, and reliability contracts. Isolating it into
its own module with platform-independent tests lets us verify the contracts
(thread-local mss, BILINEAR, no ImageGrab, Event.wait pacing, outer reconnect
loop) on any OS before we wire it into the Win32 BubbleWindow in Plan 03-02.

Output:
- `src/magnifier_bubble/capture.py` — CaptureWorker class (producer thread)
- `tests/test_capture.py` — pure-Python structural/lint tests (no mss, no Tk)
- `requirements.txt` / `requirements-dev.txt` — mss==10.1.0 pinned, installed
  and importable on the dev box
</objective>

<execution_context>
@C:/Users/Jsupport/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Jsupport/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-capture-loop/03-RESEARCH.md
@.planning/phases/03-capture-loop/03-VALIDATION.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@src/magnifier_bubble/state.py
@src/magnifier_bubble/window.py
@tests/conftest.py
@requirements.txt
@requirements-dev.txt

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from existing src/. -->
<!-- Executor should use these directly — no codebase exploration needed. -->

From src/magnifier_bubble/state.py (Phase 1 P02):
```python
class AppState:
    def capture_region(self) -> tuple[int, int, int, int, float]:
        """Returns (x, y, w, h, zoom). Lock-protected, safe to call from
        any thread. Returns primitives only — no allocations. This is the
        ONLY AppState method the capture worker should call in its hot loop.
        """
        with self._lock:
            s = self._snap
            return (s.x, s.y, s.w, s.h, s.zoom)
```

Expected CaptureWorker public API (reserved by ARCHITECTURE.md + 03-RESEARCH.md
Pattern 1):
```python
from typing import Callable, TYPE_CHECKING
if TYPE_CHECKING:
    from PIL.Image import Image as PILImage
    from magnifier_bubble.state import AppState

FrameCallback = Callable[["PILImage"], None]

class CaptureWorker(threading.Thread):
    def __init__(
        self,
        state: "AppState",
        on_frame: FrameCallback,
        target_fps: float = 30.0,
    ) -> None: ...

    def stop(self) -> None: ...       # sets self._stop Event
    def get_fps(self) -> float: ...   # rolling 60-frame average
    def run(self) -> None: ...        # the loop; inherits from Thread
    def _tick(self, sct, Image_cls) -> None: ...  # one frame
```

Pinned versions (from .planning/research/STACK.md and requirements.txt):
- mss == 10.1.0  (thread-local limitation; create mss.mss() in run(), NOT __init__)
- Pillow: pinned 11.3.0 in requirements.txt, but Pillow 12.1.1 installed on dev
  box; API-compatible for frombytes / resize / Resampling.BILINEAR / ImageTk.
  Plan's Task 1 decides whether to bump the pin.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify mss + Pillow wheels on dev box and update requirements pins</name>
  <files>requirements.txt, requirements-dev.txt</files>
  <read_first>
    - requirements.txt (current pins: Pillow==11.3.0, numpy==2.2.6, pystray==0.19.5, pyinstaller==6.11.1, pywin32==311 — mss may be missing)
    - requirements-dev.txt (Phase 1 P02 added pytest and dev tooling; this is the superset)
    - .planning/phases/03-capture-loop/03-RESEARCH.md Open Question #1 (wheel compatibility) and "Installation" section
    - .planning/STATE.md Blockers/Concerns bullet #5 (Python 3.14.3 vs pinned 3.11.9)
  </read_first>
  <action>
    Install and verify the Phase 3 runtime dependencies on the dev box, then
    reconcile the pins in requirements.txt / requirements-dev.txt with what
    actually works.

    STEP 1 — Run these verification commands in order and capture exact output
    for the SUMMARY. Use bash (the project uses forward-slash paths even on
    Windows):

        python --version
        python -m pip install "mss==10.1.0"
        python -c "import mss; print('mss', mss.__version__)"
        python -c "from PIL import Image, ImageTk; print('Pillow', Image.__version__)"
        python -c "from PIL.Image import Resampling; print('BILINEAR =', Resampling.BILINEAR.value)"

    STEP 2 — Based on the observed Pillow version:
      - If `Pillow.__version__` reports 11.3.0: leave requirements.txt as
        `Pillow==11.3.0` unchanged.
      - If `Pillow.__version__` reports 12.1.1 (or any 12.x): BUMP the pin in
        requirements.txt from `Pillow==11.3.0` to `Pillow==12.1.1` exactly
        (match the installed version). This reconciles pin with reality per
        03-RESEARCH.md Open Question #1. Record the decision in the plan
        SUMMARY.

    STEP 3 — Add `mss==10.1.0` to requirements.txt if it is not already
    present. Place it alphabetically between `numpy==2.2.6` and
    `pillow==11.3.0` (requirements.txt is case-insensitive sorted). Do NOT
    re-sort unrelated lines.

    STEP 4 — Add `mss==10.1.0` to requirements-dev.txt as well (dev file is a
    superset of runtime). If requirements-dev.txt uses `-r requirements.txt`
    at the top, the mss line only needs to live in requirements.txt — verify
    by reading the first 5 lines of requirements-dev.txt; if it does NOT
    include `-r requirements.txt`, add `mss==10.1.0` to requirements-dev.txt
    explicitly.

    STEP 5 — Do NOT install libraries globally without the pin change. Do NOT
    create a new venv. Use the current dev-box interpreter.

    STEP 6 — If Pillow 11.3.0 fails to install on Python 3.14.3 (expected per
    Open Question #1), record the failure message in the plan SUMMARY and
    leave Pillow at 12.1.1 in requirements.txt. Do NOT attempt to downgrade.
  </action>
  <verify>
    <automated>python -c "import mss, PIL; from PIL.Image import Resampling; print(mss.__version__, PIL.__version__, Resampling.BILINEAR.value)"</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "import mss; print(mss.__version__)"` prints `10.1.0`
    - `python -c "from PIL.Image import Resampling; print(Resampling.BILINEAR.value)"` prints `2`
    - `grep -E "^mss==10\.1\.0$" requirements.txt` returns exactly one match
    - `grep -E "^[Pp]illow==(11\.3\.0|12\.1\.1)$" requirements.txt` returns exactly one match (either the original pin, or the bumped 12.1.1 pin — not both)
    - `python -c "import mss; m = mss.mss(); s = m.grab(m.monitors[0]); print(len(s.rgb))"` prints a non-zero integer (proves mss import path + one-shot grab works on the dev box before we wrap it in a thread)
  </acceptance_criteria>
  <done>
    Phase 3 runtime deps are installed and importable on the dev box. mss 10.1.0
    is pinned in requirements.txt. Pillow pin matches the installed version
    (either 11.3.0 or 12.1.1, decided by what actually works). No code changes
    yet — this task is pure environment + pins.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create tests/test_capture.py with Wave 0 structural/lint tests</name>
  <files>tests/test_capture.py</files>
  <read_first>
    - tests/conftest.py (uses `win_only` marker; NO tk fixtures needed — this
      file is pure-Python, runs on any platform)
    - tests/test_winconst.py (reference: lint-style test with ast.walk, module
      loading via importlib, `from magnifier_bubble import winconst`)
    - tests/test_hit_test.py (reference: unit tests for a pure-Python module,
      `inspect.signature(compute_zone, eval_str=True)` pattern used because
      of `from __future__ import annotations` / PEP 563 — if this plan uses
      `inspect.signature` to assert CaptureWorker.__init__ signature, it MUST
      pass `eval_str=True` or the annotation comparisons will fail on string
      literals instead of real types)
    - .planning/phases/03-capture-loop/03-RESEARCH.md Pattern 1 and Validation
      Architecture > Phase Requirements → Test Map (gives the exact test names)
  </read_first>
  <behavior>
    Tests to create BEFORE capture.py exists (TDD Red). Every test must be
    runnable on any platform (no mss import at module level, no Tk, no Win32):

    - test_capture_module_imports — `from magnifier_bubble import capture` works
      without actually importing mss (capture.py must do `import mss` lazily
      inside `run()`, not at module top-level)
    - test_captureworker_class_exists — `capture.CaptureWorker` is a class that
      is a subclass of `threading.Thread`
    - test_captureworker_init_signature — signature is
      `(self, state, on_frame, target_fps=30.0)` using
      `inspect.signature(CaptureWorker.__init__, eval_str=True)`
    - test_captureworker_is_daemon_by_default — construct a fake-state/fake-cb
      worker and assert `worker.daemon is True` and `worker.name` starts with
      "magnifier-capture"
    - test_stop_is_threading_event — assert `isinstance(worker._stop, threading.Event)`
    - test_get_fps_returns_zero_before_samples — new worker, get_fps() == 0.0
    - test_get_fps_returns_positive_after_samples — manually append 60
      timestamps (1.0, 1.033, 1.066, ...) to `worker._fps_samples` and assert
      `29.0 < worker.get_fps() < 31.0`
    - test_no_imagegrab_in_capture_source — read capture.py source file, assert
      the literal substring `ImageGrab` does NOT appear anywhere (CAPT-03 lint)
    - test_capture_uses_bilinear_literal — assert the literal substring
      `Resampling.BILINEAR` appears exactly ONCE in capture.py (CAPT-04 lint)
    - test_mss_mss_constructed_inside_run — ast.walk the module, find the
      FunctionDef named `run`, walk its body, assert there is exactly one
      `mss.mss()` Call node inside run(). Also assert there is ZERO `mss.mss()`
      Call node anywhere else in the file (specifically NOT in __init__)
    - test_capture_uses_capture_region_not_snapshot — source text contains
      `state.capture_region(` at least once and `state.snapshot(` zero times
      inside the file
    - test_run_uses_event_wait_not_time_sleep — ast.walk the FunctionDef named
      `run`, assert there are at least TWO `self._stop.wait(` Call nodes (one
      for frame pacing, one for reconnect backoff) and ZERO `time.sleep(` Call
      nodes ANYWHERE inside run() (no exceptions — canonical 03-RESEARCH.md
      Example 1 uses `self._stop.wait(0.5)` for the reconnect backoff too,
      because Event.wait is interruptible and time.sleep is not)
    - test_run_has_outer_reconnect_loop — ast.walk the FunctionDef named `run`,
      assert there are at least TWO `while` statements at different nesting
      levels (outer = reconnect loop per Pitfall 7; inner = frame loop)

    Each test is deterministic, < 100ms, and uses monkeypatching or fake
    objects for AppState — NEVER import mss or Tk from this file.
  </behavior>
  <action>
    Create `tests/test_capture.py` with the 13 tests listed in the behavior
    block above. This file must run green on Linux/Mac/Windows alike because
    it contains ZERO platform-specific code.

    CRITICAL structural requirements:
    1. Module-level imports: ONLY stdlib (`ast`, `inspect`, `importlib`,
       `pathlib`, `threading`, `time`). NO mss. NO tkinter. NO PIL. NO ctypes.
    2. Use `from magnifier_bubble import capture` inside test functions OR
       at module level (the point is: importing capture.py must not pull in
       mss — capture.py's `import mss` lives inside `run()`). Add
       `test_module_import_does_not_load_mss`:

           def test_module_import_does_not_load_mss():
               import sys
               sys.modules.pop("mss", None)  # ensure fresh state
               from magnifier_bubble import capture  # noqa
               assert "mss" not in sys.modules, (
                   "capture.py imported mss at module level — mss MUST be "
                   "imported lazily inside CaptureWorker.run() for the "
                   "thread-local contract (03-RESEARCH.md Correction 2)"
               )

    3. FakeState class at module top:

           class _FakeState:
               def __init__(self, rect=(100, 100, 400, 400, 2.0)):
                   self._rect = rect
               def capture_region(self):
                   return self._rect

    4. FakeCallback as a plain `lambda img: None`.

    5. AST helpers — provide a module-level helper:

           def _capture_source() -> str:
               import magnifier_bubble.capture as mod
               return pathlib.Path(mod.__file__).read_text(encoding="utf-8")

           def _capture_ast():
               return ast.parse(_capture_source())

           def _find_funcdef(tree, name):
               for node in ast.walk(tree):
                   if isinstance(node, ast.FunctionDef) and node.name == name:
                       return node
               raise AssertionError(f"function {name} not found in capture.py")

    6. EVERY test must be named `test_<specific_behavior>` so pytest collects
       them automatically. No shared state between tests. No
       `@pytest.fixture` that touches mss / PIL / Tk.

    7. The signature test uses `inspect.signature(CaptureWorker.__init__,
       eval_str=True)` — without `eval_str=True`, `from __future__ import
       annotations` (PEP 563) makes every annotation a string and the test
       that compares `param.annotation is AppState` would fail. Reference:
       Phase 2 Plan 01 Rule 1 bug fix for `test_compute_zone_signature`.

    8. For the `test_run_uses_event_wait_not_time_sleep` test: walk the run()
       function body via `ast.walk(run_def)` and look for `ast.Call` nodes
       where `func` is an `ast.Attribute` with `attr == "sleep"`. Assert
       `len(sleep_calls) == 0` — there are NO allowed exceptions. The canonical
       03-RESEARCH.md Example 1 uses `self._stop.wait(0.5)` for the reconnect
       backoff in the outer except-block (lines 780-782), not `time.sleep()`.
       Also assert `len([c for c in ast.walk(run_def) if isinstance(c, ast.Call)
       and isinstance(c.func, ast.Attribute) and c.func.attr == "wait"
       and isinstance(c.func.value, ast.Attribute)
       and c.func.value.attr == "_stop"]) >= 2` — two `self._stop.wait(...)`
       calls (frame pacing + reconnect backoff).

    9. The `test_run_has_outer_reconnect_loop` test expects exactly 2 `ast.While`
       nodes nested inside `run`: one outer for mss reconnect (wrapping a
       `with mss.mss() as sct:`), one inner for the frame loop. Check with
       `ast.walk` + depth counting via parent tracking, or simpler: just
       assert `len([n for n in ast.walk(run_def) if isinstance(n, ast.While)]) == 2`.

    At this point capture.py does NOT exist yet, so pytest will fail with
    `ModuleNotFoundError: No module named 'magnifier_bubble.capture'`. That's
    the RED state of TDD — commit this file with the RED state, then Task 3
    makes it GREEN.
  </action>
  <verify>
    <automated>python -m pytest tests/test_capture.py --collect-only -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_capture.py` exists and is at least 120 lines
    - `python -m pytest tests/test_capture.py --collect-only -q` lists at least 13 test functions (one per behavior bullet) — collect-only must succeed even when capture.py does not exist because `from magnifier_bubble import capture` lives inside test function bodies, not module-level
    - `grep -c "^def test_" tests/test_capture.py` returns >= 13
    - `grep -E "^import mss$|^import tkinter|^from PIL" tests/test_capture.py` returns zero matches (test file has NO platform-specific imports at module level)
    - `grep -c "ImageGrab" tests/test_capture.py` returns at least 1 (the literal appears in the lint test)
    - `grep -c "Resampling.BILINEAR" tests/test_capture.py` returns at least 1 (the literal appears in the lint test)
    - `grep -c "eval_str=True" tests/test_capture.py` returns at least 1 (the signature test uses eval_str=True for PEP 563)
    - `grep -c "inspect.signature" tests/test_capture.py` returns at least 1
  </acceptance_criteria>
  <done>
    tests/test_capture.py exists with >= 13 test functions named per the
    behavior block, all using stdlib-only imports, no mss / Tk / PIL imports
    at module level. Test collection succeeds. Running the tests fails with
    ModuleNotFoundError (because capture.py has not been written yet) —
    this is the TDD RED state and is EXPECTED before Task 3.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create src/magnifier_bubble/capture.py implementing CaptureWorker</name>
  <files>src/magnifier_bubble/capture.py</files>
  <read_first>
    - src/magnifier_bubble/state.py (to confirm `capture_region()` returns
      `(x, y, w, h, zoom)` and takes `with self._lock`)
    - src/magnifier_bubble/__init__.py (must remain 0 bytes — DO NOT add an
      import of capture here; that would pull mss during the DPI-critical
      main.py import chain per STATE.md Phase 1 decisions)
    - tests/test_capture.py (just created in Task 2 — every test must pass
      after this file is written)
    - .planning/phases/03-capture-loop/03-RESEARCH.md Pattern 1 "Code Examples"
      section (has the full reference implementation at lines 714-801)
    - .planning/research/PITFALLS.md (Pitfalls 4, 12, 14 — for the docstring
      references)
    - src/magnifier_bubble/wndproc.py first 20 lines (reference for the
      `from __future__ import annotations` header + module docstring
      convention used in the project)
  </read_first>
  <behavior>
    After this task, `python -m pytest tests/test_capture.py -x` must exit 0
    on Linux/Mac/Windows. Each of the 13 behaviors from Task 2 becomes GREEN:

    - Module imports without pulling mss (lazy import inside run())
    - CaptureWorker is a subclass of threading.Thread
    - __init__ signature matches (state, on_frame, target_fps=30.0)
    - Worker is daemon and named "magnifier-capture"
    - _stop is a threading.Event()
    - get_fps returns 0.0 with < 2 samples; returns ~30 with 60 synthetic
      timestamps at 33.3 ms spacing
    - No ImageGrab substring anywhere in capture.py
    - Resampling.BILINEAR substring appears exactly once
    - mss.mss() is constructed inside run(), never in __init__
    - state.capture_region() is called inside _tick; state.snapshot() is NEVER
      called
    - run() uses self._stop.wait() for pacing, not time.sleep() (except inside
      except-block for Pitfall 7 backoff)
    - run() has an outer reconnect while-loop AND an inner frame while-loop
  </behavior>
  <action>
    Create `src/magnifier_bubble/capture.py` exactly as specified below. The
    file is ~100 lines including docstring. Copy the structure verbatim from
    03-RESEARCH.md Pattern 1 Code Example 1 (lines 714-801 of the research
    doc) with the adjustments noted.

    MANDATORY FILE CONTENTS:

    1. Module docstring — exactly these paragraphs (word-for-word), because
       the test suite greps for specific substrings:

           \"\"\"CaptureWorker — the Phase 3 30 fps producer thread.

           Reads capture-rect coordinates from AppState, grabs a region from
           the screen via mss, resizes it with Pillow BILINEAR, and marshals
           the resulting PIL.Image to the Tk main thread via
           root.after(0, on_frame, img).

           THREAD-LOCAL CONTRACT (mss 10.1.0):
               - mss.mss() instances use threading.local() for HDC/HBITMAP.
               - Instance created on thread A cannot be used from thread B.
               - FIX: create mss.mss() INSIDE run(), not __init__.
               - See .planning/phases/03-capture-loop/03-RESEARCH.md
                 Correction 2.

           HALL-OF-MIRRORS CONTRACT (mss 10.1.0):
               - mss 10.1.0 uses BitBlt(SRCCOPY | CAPTUREBLT) so layered
                 windows ARE included. CAPTUREBLT=0 is Path B defense.
               - Path A (SetWindowDisplayAffinity) lives in window.py,
                 added in Plan 03-02.
               - See 03-RESEARCH.md Correction 1.
           \"\"\"

    2. `from __future__ import annotations` (first statement after docstring).

    3. Imports at module top — stdlib ONLY:

           import threading
           import time
           from collections import deque
           from typing import Callable, TYPE_CHECKING

           if TYPE_CHECKING:
               from PIL.Image import Image as PILImage
               from magnifier_bubble.state import AppState

           FrameCallback = Callable[["PILImage"], None]

       NO `import mss` at module level. NO `from PIL import Image`.

    4. Class definition:

           class CaptureWorker(threading.Thread):
               \"\"\"30 fps screen-capture producer thread.\"\"\"

               def __init__(
                   self,
                   state: "AppState",
                   on_frame: FrameCallback,
                   target_fps: float = 30.0,
               ) -> None:
                   super().__init__(daemon=True, name="magnifier-capture")
                   self._state = state
                   self._on_frame = on_frame
                   self._target_dt = 1.0 / max(1.0, target_fps)
                   self._stop = threading.Event()
                   self._fps_samples: deque[float] = deque(maxlen=60)

               def stop(self) -> None:
                   self._stop.set()

               def get_fps(self) -> float:
                   samples = list(self._fps_samples)
                   if len(samples) < 2:
                       return 0.0
                   span = samples[-1] - samples[0]
                   if span <= 0:
                       return 0.0
                   return (len(samples) - 1) / span

    5. run() method — the hot loop with outer reconnect (Pitfall 7) and
       Event.wait pacing (Pitfall 4). THIS IS VERBATIM FROM 03-RESEARCH.md
       Example 1 (lines 759-782) — do NOT substitute any other frame-pacing
       construct. The canonical implementation uses self._stop.wait(remaining)
       for frame pacing and self._stop.wait(0.5) for reconnect backoff.
       time.sleep() MUST NOT appear in run() at all (not in the inner loop,
       not in the outer except block, not anywhere inside the run method).
       This is enforced by Task 2's test_run_uses_event_wait_not_time_sleep
       lint test — any time.sleep() Call node inside run() fails the test.

           def run(self) -> None:
               # Lazy imports — thread-local contract requires mss.mss() to
               # be created on THIS thread, so the whole mss module is also
               # imported here. Path B hall-of-mirrors defense goes BEFORE
               # mss.mss() construction.
               import mss
               import mss.windows as _mw
               _mw.CAPTUREBLT = 0
               from PIL import Image

               # Outer loop: reconnect mss on GDI failure (Pitfall 7 —
               # mss 10.1.0 GetDIBits() fails after minutes of recording).
               while not self._stop.is_set():
                   try:
                       with mss.mss() as sct:
                           while not self._stop.is_set():
                               t0 = time.perf_counter()
                               try:
                                   self._tick(sct, Image)
                               except Exception as exc:
                                   print(
                                       f"[capture] tick error: {exc}",
                                       flush=True,
                                   )
                                   break  # reconnect mss
                               self._fps_samples.append(time.perf_counter())
                               remaining = self._target_dt - (
                                   time.perf_counter() - t0
                               )
                               if remaining > 0:
                                   self._stop.wait(remaining)
                   except Exception as exc:
                       print(
                           f"[capture] mss instance error: {exc}",
                           flush=True,
                       )
                       self._stop.wait(0.5)  # backoff before retry

    6. _tick() method — source-rect math + Pillow resize:

           def _tick(self, sct, Image_cls) -> None:
               x, y, w, h, zoom = self._state.capture_region()
               if w <= 0 or h <= 0:
                   return
               # Source rect = bubble rect / zoom, centered on bubble.
               # At zoom=1.0 the grab equals the bubble rect (pass-through);
               # at zoom=6.0 the grab is 1/6 the size, centered.
               src_w = max(1, int(round(w / zoom)))
               src_h = max(1, int(round(h / zoom)))
               src_x = x + (w - src_w) // 2
               src_y = y + (h - src_h) // 2

               shot = sct.grab({
                   "left": src_x, "top": src_y,
                   "width": src_w, "height": src_h,
               })
               # shot.rgb is mss's pre-converted BGRA->RGB bytes (fastest path)
               img = Image_cls.frombytes("RGB", shot.size, shot.rgb)
               img = img.resize((w, h), Image_cls.Resampling.BILINEAR)
               self._on_frame(img)

    7. NO main-guard, NO other class, NO constants. ~100 lines total.

    DO NOT:
    - Add `import mss` at the top of the file
    - Call `mss.mss()` anywhere outside run()
    - Call `state.snapshot()` — only `state.capture_region()`
    - Call `time.sleep(` ANYWHERE inside the run() method (neither the inner
      frame loop NOR the outer except-block). Use `self._stop.wait(remaining)`
      for frame pacing and `self._stop.wait(0.5)` for reconnect backoff, per
      canonical 03-RESEARCH.md Example 1. The Task 2 lint test
      test_run_uses_event_wait_not_time_sleep walks run()'s AST and fails if
      any time.sleep() Call node exists inside run() — there is NO allowed
      exception, because the canonical example uses self._stop.wait(0.5) even
      in the except-handler backoff (Event.wait is interruptible; time.sleep
      is not — stop() must unblock instantly).
    - Prescribe any alternative like `time.sleep(max(0, target_dt - elapsed))`
      for frame pacing — this is the exact anti-pattern called out in Pitfall 4
      and is rejected by the lint test
    - Modify src/magnifier_bubble/__init__.py (must stay 0 bytes)

    After writing the file, run `python -m pytest tests/test_capture.py -x`
    and make all 13 tests green. If a lint test fails because a substring
    appears in a comment (e.g., mentioning `time.sleep` in a docstring), the
    comment/docstring must be reworded to avoid the literal token — use the
    phrase "Event.wait" in comments and refer to `sleep` only via explanatory
    prose, NOT as a raw token that the ast/grep test would catch.
  </action>
  <verify>
    <automated>python -m pytest tests/test_capture.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - File `src/magnifier_bubble/capture.py` exists with >= 80 lines
    - `grep -c "^class CaptureWorker(threading.Thread)" src/magnifier_bubble/capture.py` returns 1
    - `grep -c "def run" src/magnifier_bubble/capture.py` returns 1
    - `grep -c "def _tick" src/magnifier_bubble/capture.py` returns 1
    - `grep -c "def stop" src/magnifier_bubble/capture.py` returns 1
    - `grep -c "def get_fps" src/magnifier_bubble/capture.py` returns 1
    - `grep -c "state.capture_region(" src/magnifier_bubble/capture.py` returns >= 1
    - `grep -c "state.snapshot(" src/magnifier_bubble/capture.py` returns 0
    - `grep -c "Resampling.BILINEAR" src/magnifier_bubble/capture.py` returns 1
    - `grep -c "ImageGrab" src/magnifier_bubble/capture.py` returns 0
    - `grep -c "self._stop.wait(" src/magnifier_bubble/capture.py` returns >= 2 (one for frame pacing, one for reconnect backoff)
    - `grep -c "time.sleep(" src/magnifier_bubble/capture.py` returns 0 (NO time.sleep calls anywhere in the file — Event.wait is the only pacing construct, per canonical 03-RESEARCH.md Example 1)
    - `grep -E "^import mss$|^from mss" src/magnifier_bubble/capture.py` returns 0 matches (mss is imported lazily inside run(), not at top-level)
    - `grep -r "ImageGrab" src/magnifier_bubble/` returns 0 matches (CAPT-03 enforcement across the package)
    - `python -m pytest tests/test_capture.py -x -q` exits 0 with all 13+ tests passing
    - `wc -l src/magnifier_bubble/__init__.py` still reports 0 lines (Phase 1 invariant preserved)
    - `python -c "from magnifier_bubble import capture; print(capture.CaptureWorker.__mro__[1].__name__)"` prints `Thread`
  </acceptance_criteria>
  <done>
    CaptureWorker is fully implemented and every test in tests/test_capture.py
    passes. The module is platform-independent at import time (no mss loaded
    until run() executes on the worker thread). grep lint for ImageGrab passes
    across the whole src/magnifier_bubble/ package. CAPT-01, CAPT-02 (structure),
    CAPT-03, CAPT-04 are all code-verified. CAPT-05 / CAPT-06 remain for
    Plan 03-02 (canvas wiring + hall-of-mirrors defense).
  </done>
</task>

</tasks>

<verification>
Run the full pytest suite against the current tree — must not regress any
Phase 1 or Phase 2 tests:

  python -m pytest tests/ -x -q

Specific Phase 3 Plan 01 checks:
  python -m pytest tests/test_capture.py -v
  python -c "from magnifier_bubble import capture; w = capture.CaptureWorker.__mro__; print(w[0].__name__, w[1].__name__)"
  grep -r "ImageGrab" src/magnifier_bubble/ || echo "CAPT-03 OK: zero matches"
  grep -c "Resampling.BILINEAR" src/magnifier_bubble/capture.py  # expect 1
  python -c "import mss; print('mss wheel ok', mss.__version__)"

All must return zero exit code (except `grep -r ImageGrab` which returns 1 =
no match = the desired CAPT-03 state; the `|| echo` converts that to success).
</verification>

<success_criteria>
1. `python -m pytest tests/test_capture.py -x -q` exits 0 with at least 13
   tests passing, zero failures, zero skips (tests are pure-Python — no
   platform skipping)
2. `python -m pytest tests/ -x -q` exits 0 — no regressions in Phase 1/2 tests
3. `src/magnifier_bubble/capture.py` exists, >= 80 lines, no `import mss` at
   module top (lazy inside run() only), CaptureWorker subclasses
   threading.Thread, is daemon by default, exposes stop/get_fps/run/_tick
4. `grep -r ImageGrab src/magnifier_bubble/` returns zero matches (CAPT-03)
5. `grep -c "Resampling.BILINEAR" src/magnifier_bubble/capture.py` returns 1
   (CAPT-04 literal)
6. `mss==10.1.0` is pinned in requirements.txt and importable on the dev box
7. Pillow pin in requirements.txt matches the installed Pillow on the dev box
   (reconciled with reality per 03-RESEARCH.md Open Question #1)
8. `src/magnifier_bubble/__init__.py` remains 0 bytes (Phase 1 P01 invariant)
9. Plan 03-02 can construct `CaptureWorker(state, on_frame)` without modifying
   this file further — the public API is frozen at the end of this plan
</success_criteria>

<output>
After completion, create:
`.planning/phases/03-capture-loop/03-01-SUMMARY.md`

Include in the summary:
- The observed Python / Pillow / mss versions on the dev box
- The decision made for Open Question #1 (Pillow pin unchanged vs bumped to
  12.1.1, with the exact version installed)
- The `python -m pytest tests/test_capture.py -v` output (tail)
- The output of `grep -r "ImageGrab" src/magnifier_bubble/` (should be empty)
- Any Rule 1 bugs discovered and their fixes (follow Phase 1/2 P01-02-03 style
  "Rule 1 bug fix" entries for STATE.md decisions log)
- Confirmation that __init__.py is still 0 bytes
- The exact public API surface of CaptureWorker that Plan 03-02 will consume
</output>
